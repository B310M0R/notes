// Arrays
// array is an object with numeric names for properties

const myArray = [1, 2, 3]
console.log(myArray)

const newArray = new Array('abc', 5, 6)
console.log(newArray)
console.log(newArray.length)
console.log(newArray[0])